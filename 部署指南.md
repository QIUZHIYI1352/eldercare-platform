# 智慧养老平台 · 服务器部署指南

目标：把平台部署到一台服务器上，实现「开机自启、常驻后台、浏览器输网址/域名即可访问」。

本项目为单端口架构：FastAPI 在 `8000` 端口同时提供**前端页面 + 后端 API**，所以部署非常简单，
无需单独部署前端（无 Node 依赖）。

---

## 一、你需要准备的服务器

推荐一台 **Linux 云服务器**（阿里云 / 腾讯云 / 华为云 / AWS 等均可）：

| 配置项 | 最低建议 | 说明 |
|---|---|---|
| 系统 | Ubuntu 22.04 / 20.04（或 Debian） | 也支持 CentOS，命令略有差异 |
| CPU | 2 核 | mediapipe 姿态识别需要 CPU 算力 |
| 内存 | 4 GB | 建议 ≥2GB，跑识别时更宽裕 |
| 磁盘 | 20 GB | 存模型 + 数据库 |
| 带宽 | 按需 | 仅 Web 后台访问，流量很小 |

> 如果只是「看管理后台」不跑实时监控识别，1核2G 也够用。

---

## 二、方案 A：Docker 一键部署（推荐）

### 第 1 步：在服务器上安装 Docker

```bash
# Ubuntu / Debian
curl -fsSL https://get.docker.com | sh
sudo systemctl enable --now docker
```

安装 Docker Compose 插件：

```bash
sudo apt-get install -y docker-compose-plugin   # Ubuntu
docker compose version                          # 验证
```

### 第 2 步：把代码传到服务器

**方式 1：Git（推荐，方便以后更新）**

在服务器上：

```bash
cd /opt
git clone <你的仓库地址> eldercare-platform
cd eldercare-platform
```

**方式 2：从你本地电脑直接上传（用 scp/rsync）**

在你本地 Windows 电脑执行（假设服务器 IP 是 `1.2.3.4`，用户 `root`）：

```bash
# 在 E:\eldercare-platform 目录下
scp -r . root@1.2.3.4:/opt/eldercare-platform
```

或用 WinSCP 图形界面拖拽上传。

### 第 3 步：构建并启动

```bash
cd /opt/eldercare-platform
docker compose up -d --build
```

- 首次启动会自动下载姿态模型到 `./models`、初始化 SQLite 数据库到 `./data`
- `restart: unless-stopped` 已配置：**服务器重启后自动拉起，无需手动启动**

查看状态：

```bash
docker compose ps                 # 应为 running / healthy
docker compose logs -f web        # 查看日志
```

### 第 4 步：放行端口（云服务器安全组 + 系统防火墙）

云服务器需要在**控制台「安全组」**里放行 `8000` 端口（入方向）。

同时放行系统防火墙：

```bash
sudo ufw allow 8000/tcp     # Ubuntu
# 或
sudo firewall-cmd --add-port=8000/tcp --permanent && sudo firewall-cmd --reload   # CentOS
```

### 第 5 步：访问

浏览器打开：

```
http://服务器公网IP:8000
```

默认账号：`admin` / `admin123`

---

## 三、方案 B：不用 Docker，直接裸机跑

在服务器装好 Python 3.10~3.12：

```bash
cd /opt/eldercare-platform
pip install -r requirements.txt

# 用 systemd 做成开机自启服务
sudo tee /etc/systemd/system/eldercare.service > /dev/null <<'EOF'
[Unit]
Description=ElderCare Platform
After=network.target

[Service]
WorkingDirectory=/opt/eldercare-platform
ExecStart=/usr/bin/python3 run.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now eldercare
```

---

## 四、进阶：绑定域名 + HTTPS（推荐生产使用）

1. 域名解析：把 `eldercare.你的域名.com` 的 A 记录指向服务器公网 IP。
2. 安装 Nginx + Certbot：

```bash
sudo apt-get install -y nginx certbot python3-certbot-nginx
```

3. Nginx 反代到 8000 端口，`/etc/nginx/sites-available/eldercare`：

```nginx
server {
    listen 80;
    server_name eldercare.你的域名.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # WebSocket（若前端用到实时推送）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/eldercare /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
sudo certbot --nginx -d eldercare.你的域名.com   # 自动签发 HTTPS 证书
```

之后即可用 `https://eldercare.你的域名.com` 访问（自动续期）。

---

## 五、可选：在服务器上跑实时监控识别（RTSP 无头模式）

编辑 `docker-compose.yml` 里 monitor 服务的 `MONITOR_SOURCE`（如 `rtsp://user:pwd@ip:554/stream1`）和 `MONITOR_PROCESS`，然后：

```bash
docker compose --profile monitor up -d
docker logs -f eldercare-monitor
```

---

## 六、常见问题

| 问题 | 排查 |
|---|---|
| 浏览器打不开 | `docker compose ps` 看是否 running；查安全组/防火墙是否放行 8000 |
| 端口被占用 | `sudo lsof -i:8000` 找到占用进程 |
| 模型下载慢 | 首次启动需下载 pose 模型，耐心等待；可看 `docker compose logs -f web` |
| 更新代码后 | `git pull && docker compose up -d --build`（数据在 ./data，不会丢） |
| 备份数据 | 备份 `./data` 目录即可（SQLite 数据库） |

---

## 七、安全建议

- 上线后**立即修改默认密码** `admin123`
- 建议开启 HTTPS（见第四节）
- 若只对内网使用，可在安全组里把 8000 端口限制为你的 IP
