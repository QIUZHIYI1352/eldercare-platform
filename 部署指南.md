# 智慧实训规范平台 · 部署指南

> 当前版本技术栈：FastAPI + MySQL 8 + 原生前端 + MediaPipe；单端口 8000 同时提供页面与接口。
> 需要逐条复制粘贴的命令，见《服务器部署命令清单.md》；本文档说明原理与选项。

## 一、服务器要求

| 项目 | 建议 |
|---|---|
| 系统 | Ubuntu 22.04 LTS |
| CPU/内存 | 2 核 4G（最低 2 核 2G，需加 swap） |
| 磁盘 | 40G |
| 带宽 | 3Mbps 以上 |
| 公网 IP | 建议绑定弹性公网 IP（EIP），避免停机后地址变化 |

安全组放行：`22`（SSH）、`8000`（平台）。

## 二、Docker 部署（推荐）

1. 安装 Docker（国内走阿里云镜像，命令见命令清单第 2 步）
2. 配置镜像加速（命令清单第 3 步），否则拉取 `mysql:8.0` 会超时
3. 拉取代码并启动：

```bash
cd ~
git clone https://github.com/QIUZHIYI1352/eldercare-platform.git
cd eldercare-platform
echo 'MYSQL_ROOT_PASSWORD=你的强密码' > .env
docker compose up -d --build
```

启动后会自动创建 MySQL 并写入种子数据；访问 `http://<公网IP>:8000`，默认账号 `admin / admin123`（登录后立即修改）。

数据保存在 Docker 卷 `mysql_data` 中，`docker compose down` 不会丢数据；只有 `down -v` 会删除数据卷。

## 三、本机运行（开发/自用）

```bash
pip install -r requirements.txt
# Windows PowerShell 示例
$env:MYSQL_PASSWORD = "你的MySQL密码"
python run.py
```

访问 `http://localhost:8000`。本机需先安装 MySQL 8 并保证 3306 可连。

## 四、域名 + HTTPS（可选，长期使用建议）

1. 域名 A 记录指向服务器公网 IP（国内服务器使用域名需完成 ICP 备案）。
2. 安装 Nginx 与 Certbot，反向代理到 `127.0.0.1:8000`：

```nginx
server {
    listen 80;
    server_name your.domain.com;
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

3. `certbot --nginx -d your.domain.com` 自动签发 HTTPS 证书。

若后续要做「学员用自己电脑摄像头」，浏览器采集需要 HTTPS 环境。

## 五、实时监控容器（RTSP 摄像头）

服务器没有本机摄像头时，用 RTSP 网络摄像头跑常驻识别：

```bash
# 先修改 docker-compose.yml 中 monitor 服务的 MONITOR_SOURCE / MONITOR_PROCESS
docker compose --profile monitor up -d
docker logs -f eldercare-monitor
```

## 六、常见问题

| 问题 | 处理 |
|---|---|
| 浏览器打不开 | 实例是否运行、安全组是否放行 8000、`docker compose ps` 是否 healthy |
| 拉镜像超时 | 配置 `/etc/docker/daemon.json` 加速地址后 `systemctl restart docker` |
| 构建卡在 apt/pip | 使用仓库自带 Dockerfile（已配置清华源）；`Ctrl+C` 后重新 `docker compose up -d --build` |
| 公网 IP 变化 | 绑定 EIP 固定地址，并重新通知使用者 |
| 更新后界面没变 | 服务器 `git pull && docker compose up -d --build`，浏览器 `Ctrl+Shift+R` 强刷 |
| 备份数据 | `docker compose exec mysql mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" eldercare > backup.sql` |

## 七、安全建议

1. 上线后立即修改 `admin` 默认密码。
2. 不希望任何人注册时，后续可关闭开放注册（目前开放注册仅限学员角色）。
3. 长期使用建议加域名 + HTTPS，并定期备份数据库。
4. 不要把数据库 3306 端口暴露到公网。
