# 服务器部署 · 可直接复制的命令清单

> 适用：阿里云 Windows 服务器，IP `120.26.23.146`
> 使用方式：远程桌面登录服务器后，**逐块复制粘贴到 cmd 或 PowerShell** 执行。
> 全程约 15~20 分钟（大部分时间在下载依赖和模型）。

---

## 第 0 步：远程桌面登录服务器

1. 你电脑上按 `Win + R`，输入 `mstsc` 回车。
2. 计算机填 `120.26.23.146` → 连接 → 输入阿里云设置的管理员账号密码。

---

## 第 1 步：安装 Python 3.12 + Git

**在服务器浏览器打开这两个下载页：**

- Python 3.12（64 位）：https://www.python.org/ftp/python/3.12.7/python-3.12.7-amd64.exe
- Git：https://git-scm.com/download/win

**Python 安装时务必勾选 ✅ `Add python.exe to PATH`**，其余一路下一步。
Git 一路下一步即可。

装完开一个**新的** cmd，验证（分别应显示版本号）：

```bat
python --version
git --version
```

> 若 `python --version` 报「不是内部或外部命令」，说明没勾 Add to PATH，
> 重装一次或手动把 `C:\Python312` 加入系统 PATH。

---

## 第 2 步：阿里云安全组放行 8000（在阿里云控制台，不是服务器上）

阿里云控制台 → 云服务器 ECS → 实例 `120.26.23.146` → **安全组** → 点安全组 ID → **入方向** → **手动添加**：

| 字段 | 填 |
|---|---|
| 授权策略 | 允许 |
| 优先级 | 1 |
| 协议类型 | 自定义 TCP |
| 端口范围 | `8000/8000` |
| 授权对象 | `0.0.0.0/0`（先全开，测通后再收紧） |
| 描述 | 智慧养老平台 |

保存。

---

## 第 3 步：拉取代码

在服务器 cmd 执行（**整块复制**）：

```bat
cd C:\
git clone https://github.com/QIUZHIYI1352/eldercare-platform.git
cd eldercare-platform
```

> 若仓库是 **Private**，clone 时会要登录：用户名填 `QIUZHIYI1352`，密码填你的
> GitHub Token（`ghp_***REMOVED***`，或新生成的）。
> 登录一次后 Git 会记住凭据。

---

## 第 4 步：安装依赖

```bat
cd C:\eldercare-platform
pip install -r requirements.txt
```

> 约 2~5 分钟。若 pip 慢，可加国内镜像：
> `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`
>
> 若报 `Microsoft Visual C++` 相关错误，装这个运行库后重试：
> https://aka.ms/vs/17/release/vc_redist.x64.exe

---

## 第 5 步：首次启动测试（关键验证）

```bat
cd C:\eldercare-platform
python run.py
```

看到这行即成功：

```
Uvicorn running on http://0.0.0.0:8000
```

此时**保持这个窗口别关**，在服务器本机浏览器打开 `http://localhost:8000`：
能进登录页（账号 `admin` / 密码 `admin123`）就说明一切正常。

> 首次启动会自动下载姿态模型到 `models\`、生成数据库 `data\eldercare.db`，等几秒即可。

测试没问题后，按 `Ctrl + C` 停掉，进入下一步。

---

## 第 6 步：放行 Windows 防火墙（必须）

以**管理员身份**打开 cmd（开始菜单搜 cmd → 右键 → 以管理员身份运行），执行：

```bat
netsh advfirewall firewall add rule name="ElderCare 8000" dir=in action=allow protocol=TCP localport=8000
```

---

## 第 7 步：设为开机自启（后台常驻、无窗口）

**先确认 Python 实际安装路径**（管理员 cmd 执行）：

```bat
where python
```

会输出类似 `C:\Python312\python.exe`（也可能是 `C:\Users\...\AppData\...\python.exe`）。
**把下面命令里的路径改成你查到的路径**，然后执行：

```bat
schtasks /Create /TN "ElderCarePlatform" /TR "C:\Python312\python.exe C:\eldercare-platform\run.py" /SC ONSTART /RU SYSTEM /RL HIGHEST /F
```

- `/SC ONSTART`：开机即启动，无需登录桌面
- `/RU SYSTEM`：以系统身份后台运行

手动立即启动一次（验证自启配置是否有效）：

```bat
schtasks /Run /TN "ElderCarePlatform"
```

等 5 秒后查状态：

```bat
schtasks /Query /TN "ElderCarePlatform"
```

> 状态显示「正在运行」即成功。

---

## 第 8 步：公网验证

在你自己的电脑浏览器打开：

```
http://120.26.23.146:8000
```

看到登录页 = **部署完成** ✅　账号 `admin` / 密码 `admin123`。

---

## 排查速查

| 现象 | 处理 |
|---|---|
| 浏览器打不开 | ① 安全组有没有放行 8000？② 第 6 步防火墙命令执行了吗？③ 服务是否在跑：`schtasks /Query /TN "ElderCarePlatform"` |
| `python` 不是命令 | 重装 Python 勾选 Add to PATH，或重开 cmd |
| `pip install` 报错 | 加镜像源；装 VC++ 运行库（见第 4 步） |
| 端口被占用 | `netstat -ano | findstr :8000` 找 PID 后 `taskkill /PID xxx /F` |
| 想看运行日志 | 服务日志在系统里，排查时可直接前台跑 `python run.py` 看输出 |
| 更新代码 | `cd C:\eldercare-platform` → `git pull` → `schtasks /Run /TN "ElderCarePlatform"` |

---

## 上线必做（安全）

1. 登录后台 → **改掉默认密码 `admin123`**。
2. 安全组「授权对象」从 `0.0.0.0/0` 改为你自己的出口 IP（百度搜「IP」看到的那串 + `/32`）。
