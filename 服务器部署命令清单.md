# 服务器部署 · 命令清单（Linux + Docker + MySQL）

> 适用：Ubuntu 22.04 云服务器（阿里云/腾讯云/华为云均可）
> 本文档为当前版本（MySQL 版）的唯一操作清单，逐块复制粘贴到 SSH 终端即可。
> 全程约 10 分钟（首次构建 3~6 分钟）。

## 第 0 步：在云控制台准备（不是服务器上）

1. 实例状态：**运行中**。
2. 安全组入方向放行：

| 端口 | 用途 | 授权对象 |
|---|---|---|
| 22 | SSH | 0.0.0.0/0 |
| 8000 | 平台 Web | 0.0.0.0/0 |

3. **强烈建议绑定弹性公网 IP（EIP）**：否则实例停机再开机后公网 IP 可能变化，旧链接会失效，必须重新通知所有使用者。

## 第 1 步：SSH 登录服务器

```bash
ssh root@<你的公网IP>
```

## 第 2 步：安装 Docker（走阿里云镜像，国内可直连）

```bash
curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://mirrors.aliyun.com/docker-ce/linux/ubuntu jammy stable" > /etc/apt/sources.list.d/docker.list
apt update && apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
systemctl enable --now docker
docker compose version
```

## 第 3 步：配置镜像加速（否则拉 mysql/python 镜像会超时）

```bash
mkdir -p /etc/docker
cat > /etc/docker/daemon.json <<'EOF'
{"registry-mirrors":["https://docker.m.daocloud.io","https://docker.1panel.live"]}
EOF
systemctl restart docker
docker info | grep -A3 "Registry Mirrors"
```

> 若这两个加速站不通：阿里云控制台 → 容器镜像服务 → 镜像加速器，用你的专属地址替换后重启 Docker。

## 第 4 步：拉取代码

```bash
cd ~
git clone https://github.com/QIUZHIYI1352/eldercare-platform.git
cd eldercare-platform
```

## 第 5 步：设置数据库密码并启动

```bash
echo 'MYSQL_ROOT_PASSWORD=你的强密码' > .env
docker compose up -d --build
docker compose ps
```

首次构建 3~6 分钟（Dockerfile 已内置清华 apt/pip 源）。等 web 状态变为 `healthy`。

## 第 6 步：验证

```bash
curl http://127.0.0.1:8000/api/health
```

返回 `{"status":"ok",...}` 即部署成功，浏览器访问：

```
http://<你的公网IP>:8000
```

默认账号 `admin / admin123`。

## 第 7 步：登录后立即改管理员密码

```bash
cd ~/eldercare-platform
docker compose exec web python -c "from backend import auth, database as db; db.execute('UPDATE users SET password_hash=? WHERE username=?', (auth.hash_password('新密码'), 'admin')); print('done')"
```

## 日常运维

```bash
docker compose ps                              # 状态
docker compose logs -f web                     # 日志（Ctrl+C 退出查看）
docker compose restart web                     # 重启 Web
docker compose down                            # 停止（数据保留在 mysql_data 卷）
docker compose up -d --build                   # 更新代码后重建
docker compose exec mysql mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" eldercare > backup.sql   # 备份数据库
```

服务器重启后容器会自动拉起（`restart: unless-stopped`），无需手动操作。

## 更新代码

本地改完并推送后，在服务器执行：

```bash
cd ~/eldercare-platform
git pull
docker compose up -d --build
```

## 排查速查

| 现象 | 处理 |
|---|---|
| 浏览器打不开 | ① 实例是否运行；② 安全组是否放行 8000；③ `docker compose ps` 是否 healthy |
| 拉镜像超时 | 检查 `/etc/docker/daemon.json` 加速地址，`systemctl restart docker` 后重试 |
| `curl` 连接被重置 | web 还在启动，等 30 秒再试；`docker compose logs --tail=80 web` |
| 公网 IP 变了 | 旧链接失效；绑定 EIP 固定地址 |
| 数据库密码忘记 | 修改 `.env` 后 `docker compose down && docker compose up -d`（仅首次初始化生效，改已初始化实例需用 SQL 改密） |
